<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:weightSum="10">
<LinearLayout
    android:layout_width="match_parent"
    android:layout_height="0dp"
    android:layout_weight="1"
    android:background="@color/white"
    android:orientation="horizontal"
    android:weightSum="10">
    <TextView
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="8"
        android:background="@color/decoriss"
        android:text="@string/goods_info"
        android:id="@+id/name_of_currrent_product"
        android:layout_gravity="right"
        android:gravity="center_vertical"
        android:textColor="@color/black"
        />
    <ImageButton
        android:layout_width="0dp"
        android:layout_gravity="center_horizontal|center_vertical"
        android:layout_height="match_parent"
        android:layout_weight="2"
        android:id="@+id/back_full_screen"
        android:src="@mipmap/ic_back"
        android:background="@color/decoriss"/>

</LinearLayout>
           <ListView
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="9"
                android:scrollbars="vertical"
                android:id="@+id/lv_productOption"
                android:background="@color/white"/>

    <!--<RatingBar-->
        <!--android:paddingTop="5dp"-->
        <!--android:layout_gravity="center_horizontal|center_vertical"-->
        <!--android:layout_width="wrap_content"-->
        <!--android:isIndicator="true"-->
        <!--android:id="@+id/rank_of_product"-->
        <!--style="@style/goodRatingBar"-->
        <!--android:numStars="5"-->
        <!--android:layout_height="0dp"-->
        <!--android:layout_weight="2"-->
        <!--/>-->
</LinearLayout>